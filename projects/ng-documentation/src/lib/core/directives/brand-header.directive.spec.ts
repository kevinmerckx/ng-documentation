import { BrandHeaderDirective } from './brand-header.directive';

describe('BrandHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new BrandHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
